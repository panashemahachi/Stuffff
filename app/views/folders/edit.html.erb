<h1>Raname Folder</h1>
<%= render :partial => "info/breadcrumbs" %> 

<%= render 'form' %>

<p> 
<% if @folder.parent %> 
   <%= link_to "Back to '#{@folder.parent.name}' Folder", browse_path(@folder.parent) %> 
<% else %> 
   <%= link_to "Back", root_url %> 
<% end %> 
</p>
